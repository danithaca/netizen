Resources:

Main code base: SI North / Home
Databases: None (data exported to CSV)

Downloading: SIN (Home)


Problems:
* BeautifulSoup can't handle all the cases and don't throw errors, just ignore the rest of the document. e.g. 100002.shtml (<img> has error).
